# Variables de entorno para el backend
# Copia este archivo como backend/.env y completa los valores

# ─── Base de datos ────────────────────────────────────────────────────────────
# Opción A: URL completa (Render, Railway, Supabase, etc.)
DATABASE_URL=postgresql://usuario:contraseña@host:5432/nombre_db

# Opción B: Variables individuales (Docker Compose local)
# DB_HOST=db
# DB_PORT=5432
# DB_NAME=catalogo_artesanias
# DB_USER=postgres
# DB_PASSWORD=CambiarPorUnaContraseñaSegura123!

# ─── Servidor ────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development

# ─── Seguridad ───────────────────────────────────────────────────────────────
# Genera con: openssl rand -base64 32
JWT_SECRET=genera_un_secreto_largo_de_al_menos_32_caracteres_aqui

# URL del frontend (sin barra final). En Render: tu URL del servicio web
FRONTEND_URL=http://localhost:5173

# ─── Archivos ────────────────────────────────────────────────────────────────
UPLOAD_DIR=./uploads

# ─── Admin inicial ───────────────────────────────────────────────────────────
# Solo usado por el script scripts/create-admin.js
ADMIN_USER=admin
ADMIN_EMAIL=admin@tudominio.com
ADMIN_PASSWORD=CambiarPorContraseñaSegura123!

# ─── Logging ─────────────────────────────────────────────────────────────────
LOG_LEVEL=info
