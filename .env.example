# Docker Compose - Copia a .env en la raíz del proyecto
# cp .env.example .env
# Luego edita con valores reales (no uses admin, postgres, password)

DB_PASSWORD=contraseña_segura_postgres
JWT_SECRET=genera_con_openssl_rand_base64_32
