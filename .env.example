# Variables de entorno para desarrollo local con Docker Compose
# Copia este archivo como .env y completa los valores

# Contraseña de la base de datos PostgreSQL (mínimo 12 caracteres)
DB_PASSWORD=CambiarPorUnaContraseñaSegura123!

# JWT Secret (genera con: openssl rand -base64 32)
JWT_SECRET=genera_un_secreto_largo_de_al_menos_32_caracteres_aqui

# Entorno (development | production)
NODE_ENV=development
